// function in ts

// Adds types to parameters and return values

// Errors are caught before running (compile-time)

// More predictable and safer code

function addTwo(num){
    return num + 2
}
addTwo( 5)


// export{}